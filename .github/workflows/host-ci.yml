name: Host CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    name: Build and test
    runs-on: macos-latest

    defaults:
      run:
        working-directory: host/macos-hid-inspector

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show toolchain
        run: |
          swift --version
          xcrun --find swift

      - name: Install SwiftFormat
        run: brew install swiftformat

      - name: Lint Swift (SwiftFormat --lint)
        working-directory: ${{ github.workspace }}
        run: swiftformat --lint host/macos-hid-inspector

      - name: Build SwiftPM package
        run: swift build

      - name: Run XCTest target
        run: swift test

      - name: Run inline selftest binary
        run: swift run bluetrack-hid-inspector selftest

      - name: Set up Python for cross-platform golden vectors
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python deps (cryptography for vectors, black for lint)
        run: pip install --upgrade pip && pip install cryptography black
        working-directory: ${{ github.workspace }}

      - name: Lint Python (black --check)
        working-directory: ${{ github.workspace }}
        run: |
          python3 -m black --check \
            android/tools/ble_encrypt_sender.py \
            host/test-vectors/generate_vectors.py \
            host/test-vectors/test_golden_vectors.py

      - name: Regenerate golden vectors fixture (must be byte-identical to committed file)
        working-directory: ${{ github.workspace }}
        run: |
          python3 host/test-vectors/generate_vectors.py
          git diff --exit-code -- host/test-vectors/feedback_v1.json

      - name: Run cross-platform golden-vector tests (Python)
        working-directory: ${{ github.workspace }}
        run: python3 host/test-vectors/test_golden_vectors.py
